<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:SantaSecilia.ViewModels"
    x:Class="SantaSecilia.Views.EditarTrabajadorPage"
    Title="Component Header x3"
    BackgroundColor="#FAF4C6">

    <ContentPage.BindingContext>
        <vm:TrabajadorFormViewModel/>
    </ContentPage.BindingContext>
    
    <Grid>
        <!-- Card principal -->
        <Border
            Stroke="#7A9E7E"
            StrokeThickness="2"
            BackgroundColor="White"
            StrokeShape="RoundRectangle 28"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            WidthRequest="540">
            
            <VerticalStackLayout Spacing="0">

                <!-- HEADER -->
                <Border
                    BackgroundColor="#CDE8C5"
                    StrokeShape="RoundRectangle 18,18,0,0"
                    Padding="12">
                    <Label
                        Text="Editar Trabajador"
                        FontSize="20"
                        FontAttributes="Bold"
                        HorizontalOptions="Center"/>
                </Border>

                <!-- CONTENIDO -->
                <Grid Padding="20"
                      ColumnDefinitions="*,220">

                    <!-- FORM -->
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Nombre Completo" FontSize="12"/>
                        <Border Stroke="#CCCCCC" StrokeShape="RoundRectangle 0">
                            <Entry
                                Text="{Binding Nombre}"
                                BackgroundColor="Transparent"/>
                        </Border>

                        <Label Text="Cédula" FontSize="12"/>
                        <Border Stroke="#CCCCCC" StrokeShape="RoundRectangle 10">
                            <Entry
                                Text="{Binding Cedula}"
                                BackgroundColor="Transparent"/>
                        </Border>

                        <Label Text="Estado" FontSize="12"/>
                        <Border Stroke="#CCCCCC" StrokeShape="RoundRectangle 10">
                            <Picker
                                ItemsSource="{Binding Estados}"
                                BackgroundColor="Transparent"/>
                        </Border>

                        <!-- BOTONES -->
                        <HorizontalStackLayout Spacing="20" Margin="10,10,0,0">
                            <Button
                                Text="Guardar"
                                BackgroundColor="#64B5F6"
                                TextColor="Black"
                                CornerRadius="15"
                                WidthRequest="120"
                                Clicked="OnCancelarClicked"/>

                            <Button
                                Text="Cancelar"
                                BackgroundColor="#F28B82"
                                TextColor="Black"
                                CornerRadius="15"
                                WidthRequest="120"
                                Clicked="OnCancelarClicked"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <!-- IMAGEN -->
                    <Image
                        Grid.Column="1"
                        Source="logo.png"
                        Aspect="AspectFit"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"/>
                </Grid>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>