<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    BackgroundColor="#FAF4C6"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:SantaSecilia.ViewModels"
    x:Class="SantaSecilia.Views.TrabajadoresListPage">

    <ContentPage.BindingContext>
        <vm:TrabajadoresListViewModel/>
    </ContentPage.BindingContext>

    <Grid Padding="20"
      RowDefinitions="Auto,Auto,Auto,*"
      RowSpacing="15">

        <!-- Título -->
        <Label Grid.Row="0"
           Text="Gestionar Trabajadores"
           TextColor="Black"
           FontSize="26"
           FontAttributes="Bold"/>

        <!-- Buscador -->
        <Border Grid.Row="1"
            Stroke="#DDD"
            StrokeThickness="1"
            BackgroundColor="White"
            StrokeShape="RoundRectangle 12">
            <SearchBar Placeholder="Buscar trabajador..."
                   BackgroundColor="Transparent"/>
        </Border>

        <!-- Botón -->
        <Button Grid.Row="2"
            Text="+ Registrar Trabajador"
            Command="{Binding RegistrarCommand}"
            HorizontalOptions="End"
            BackgroundColor="#4CAF50"
            TextColor="White"
            CornerRadius="12"
            Padding="12,6"/>

        <!-- LISTA SCROLLEABLE -->
        <CollectionView Grid.Row="3"
                    ItemsSource="{Binding Trabajadores}">

            <CollectionView.Header>
                <Border BackgroundColor="#2F7F89"
                    StrokeShape="RoundRectangle 12"
                    Padding="10">
                    <Grid ColumnDefinitions="1*,2*,2*,1*,1*">
                        <Label Text="Código" TextColor="White"/>
                        <Label Grid.Column="1" Text="Nombre Completo" TextColor="White"/>
                        <Label Grid.Column="2" Text="Cédula" TextColor="White"/>
                        <Label Grid.Column="3" Text="Estado" TextColor="White"/>
                    </Grid>
                </Border>
            </CollectionView.Header>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,6"
                        Padding="12"
                        BackgroundColor="White"
                        Stroke="#E0E0E0"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 14">

                        <Grid ColumnDefinitions="1*,2*,2*,1*,1*">
                            <Label Text="{Binding Codigo}" TextColor="Black"/>
                            <Label Grid.Column="1" Text="{Binding Nombre}" TextColor="Black"/>
                            <Label Grid.Column="2" Text="{Binding Cedula}" TextColor="Black"/>
                            <Label Grid.Column="3" Text="{Binding Estado}" TextColor="Black"/>

                            <Button Grid.Column="4"
                                Text="Editar"
                                BackgroundColor="#1976D2"
                                TextColor="White"
                                CornerRadius="10"
                                FontSize="12"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditarCommand}"
                                CommandParameter="{Binding .}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>