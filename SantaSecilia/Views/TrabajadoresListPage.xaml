<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    BackgroundColor="#FAF4C6"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:SantaSecilia.ViewModels"
    xmlns:views="clr-namespace:SantaSecilia.Views"
    x:Class="SantaSecilia.Views.TrabajadoresListPage">

    <ContentPage.BindingContext>
        <vm:TrabajadoresListViewModel/>
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto, *">

        <!-- Header fuera del padding -->
        <views:HeaderView Grid.Row="0"
                      x:Name="MyHeader"/>

            <Grid   Grid.Row="1" 
                Padding="20"
                RowDefinitions="Auto,Auto,Auto,*"
                RowSpacing="15">

            <!-- Título -->
            <Label Grid.Row="0"
                Text="Gestionar Trabajadores"
                TextColor="Black"
                FontSize="26"
                FontAttributes="Bold"/>

            <!-- Buscador -->
            <Border Grid.Row="1"
                Stroke="#DDD"
                StrokeThickness="1"
                BackgroundColor="White"
                StrokeShape="RoundRectangle 12"
                HorizontalOptions="Start"
                WidthRequest="500">
                    <SearchBar Placeholder="Buscar trabajador..."
                        TextColor="Black"
                       BackgroundColor="Transparent"/>
            </Border>

            <!-- Botón -->
                <Button Grid.Row="1"
                    Text="+ Registrar Trabajador"
                    Command="{Binding RegistrarCommand}"
                    HorizontalOptions="End"
                    BackgroundColor="#B2FBA5"
                    TextColor="Black"
                    CornerRadius="12"
                    Padding="12,6"/>

            <!-- LISTA SCROLLEABLE -->
            <CollectionView Grid.Row="3"
                    ItemsSource="{Binding Trabajadores}">

                <CollectionView.Header>
                    <Border BackgroundColor="#80BFFF"
                    StrokeShape="RoundRectangle 12"
                    Padding="10">
                        <Grid ColumnDefinitions="1*,2*,2*,1*,1*">
                            <Label Text="Código" TextColor="Black"/>
                            <Label Grid.Column="1" Text="Nombre Completo" TextColor="Black"/>
                            <Label Grid.Column="2" Text="Cédula" TextColor="Black"/>
                            <Label Grid.Column="3" Text="Estado" TextColor="Black"/>
                        </Grid>
                    </Border>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,6"
                        Padding="12"
                        BackgroundColor="White"
                        Stroke="#E0E0E0"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 14">

                            <Grid ColumnDefinitions="1*,2*,2*,1*,1*">
                                <Label Text="{Binding Codigo}" TextColor="Black"/>
                                <Label Grid.Column="1" Text="{Binding Nombre}" TextColor="Black"/>
                                <Label Grid.Column="2" Text="{Binding Cedula}" TextColor="Black"/>
                                <Label Grid.Column="3" Text="{Binding Estado}" TextColor="Black"/>

                                <Button Grid.Column="4"
                                Text="Editar"
                                BackgroundColor="#80BFFF"
                                TextColor="Black"
                                CornerRadius="10"
                                FontSize="12"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditarCommand}"
                                CommandParameter="{Binding .}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>